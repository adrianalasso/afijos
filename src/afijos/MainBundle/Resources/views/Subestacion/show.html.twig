{% extends "MainBundle::show.html.twig" %}
{% block titulo %}Subestaciones{%endblock%}

{%block entidad%}
<i class="icon-exchange exchange-icon"></i>
<a href="{{ path('subestacion') }}">Subestaciones</a>
{%endblock%}

{%block mapatitulo%}Ver Subestación{%endblock%}
{%block pagetitulo%}Subestaciones{%endblock%}
{%block pagedetalle%}Ver Subestación{%endblock%}


{%block datos%}    
<div class="profile-info-row">
            <div class="profile-info-name"> Código </div>
            <div class="profile-info-value">
                    <span >
                        {{ entity.id }}
                    </span>
            </div>
    </div>

    <div class="profile-info-row">
            <div class="profile-info-name"> Nombre </div>
            <div class="profile-info-value">
                <span>
                {{ entity.nombre }}
                </span>
            </div>
    </div>                                 
{%endblock%}
{%block linkeliminar%}
<a href="#"
{%endblock%}  
{%block link%}
<a href="{{ path('subestacion') }}"
{%endblock%}
{%block linkedit%}
<a href="{{ path('subestacion_edit', { 'id': entity.id }) }}" 
{%endblock%}
   
{%block plugins%}
{{parent()}}
{%endblock%}

{%block scripthijo%}
<script type="text/javascript">
window.onload = function(){
    var elemento=document.getElementById("mnsubestaciones");
    elemento.classList.add('active');
    elemento.parentNode.parentNode.classList.add('active');
    elemento.parentNode.parentNode.classList.add('open');
};
$(function() {
    $("#bteliminar").on(ace.click_event, function() {
            bootbox.confirm("Estas seguro de eliminar registro?", function(result) {
                if(result){
                    location.href="{{ path('subestacion_eliminar', { 'id': entity.id }) }}";
                }
            });
    });
});
</script>
{%endblock%}

